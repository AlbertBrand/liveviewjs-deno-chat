# LiveViewJS Deno 'standalone' chat example

A rudimentary chat client that is built with LiveViewJS. The project is mostly standalone, although it contains some
copied files from the original LiveViewJS repo.

# Setup

```
npm install
npm run buildClient
```

# Run

```
npm run start
```
